/**
 * infer 关键字
 */

/**  
 * 语句 let num 中，通过 let 来声明了一个变量，那怎样声明一个不确定的类型变量呢？ 
 * 答案是使用 infer 关键字，infer R 就是声明了一个类型变量 R。 
 */

const inferGssg = ():void=>{
  console.log(2323232323);
}

inferGssg();

export default inferGssg;